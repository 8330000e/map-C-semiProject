server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

<<<<<<< Updated upstream
    # API 요청을 백엔드로 전달
    location /api/ {
        # 도커 내부 DNS 사용
        resolver 127.0.0.11 valid=120s;
=======
    location /api/ {
        resolver 127.0.0.11 valid=30s;  # 도커 내부 DNS 서버 주소
        set $backend "http://semi-backend:8080"; 
        proxy_pass $backend;
    }

    location ~* ^/(api|undefined)/(.*) {
        rewrite ^/(api|undefined)/(.*) /$2 break;
>>>>>>> Stashed changes
        
        # 'semi-backend'는 실행 중인 컨테이너 이름이어야 합니다
        set $target "http://semi-backend:9999";
    	proxy_pass $target;
        
        # 타임아웃 시간을 넉넉하게 설정
        proxy_connect_timeout 300s;
        proxy_read_timeout 300s;
        proxy_send_timeout 300s;
        
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}