<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="kr.co.iei.member.model.dao.MemberDao">

	<insert id="insertMember"
		parameterType="kr.co.iei.member.model.vo.Member">
		INSERT INTO member_tbl1
		(member_id, member_pw, member_name, member_grade, member_nickname, member_email,
		member_thumb,
		enroll_date, member_status, manner_score, trade_count)
		VALUES
		(#{memberId}, #{memberPw}, #{memberName}, 2, #{memberNickname},
		#{memberEmail}, #{memberThumb}, SYSDATE, 1,
		0, 0)
	</insert>
	
	<select id="selectOneMember" resultType="member">
		select * from member_tbl1
		where member_id =#{memberId}

		
	</select>
	<update id="updateMemberInfo">
		update member_tbl set member_name=#{memberName}, member_nickname=#{memberNickname}, member_email=#{memberEmail}
		where member_id=#{memberId}
	</update>
	<select id="getOneMemberInfo">
		select member_name,member_email,enroll_date,member_status from member_tbl1 where member_id = #{memberId} 
	</select>
</mapper>
