# Dependency directories (라이브러리 제외)
node_modules/
jspm_packages/

# Build outputs (빌드 결과물 제외)
dist/
build/
.next/
out/

# Environment variables (보안 키 - 가장 중요!)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Debug logs (로그 파일)
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs
*.log

# OS Metadata (운영체제 생성 파일)
.DS_Store
Thumbs.db

# IDE/Editor folders (에디터 설정)
.vscode/
.idea/
*.swp
*.scratch

# Optional: Coverage reports (테스트 결과)
coverage/